# Plugin SDK v1.1 transcript provenance

These are synthetic, deterministic transcripts generated by
`pkg/pluginapi/sdk/server_test.go` through the public length-prefixed
`pluginapi.Codec`. The envelopes use reserved `.invalid` URLs and contain no
credentials, production identities, or copied upstream content.

They were introduced against repository base commit
`9c95d6584de878a5579d9fd5d84c3c0449636d2a`. No source or fixture material was
derived from the pinned Python yt-dlp checkout, and neither the SDK nor the
tests require Python or that checkout.

| Fixture | Negotiation path | SHA-256 |
| --- | --- | --- |
| `transcript-v1.0.json` | Legacy enumerated v1.0 host to v1.0-v1.1 plugin | `5488108b61fa8f685a7ff3857f84e7652410cd308d9a8711d77e0618ecc77f40` |
| `transcript-v1.1.json` | Enumerated plus ranged v1.1 host to v1.0-v1.1 plugin | `f4bdef4da932e7d7d3ed0782ecce10d9c0333e0a5b7aeef2408b71cd4ce566db` |

The tests reconstruct each logical transcript, compare every canonical JSON
envelope byte, and transport the exchange through real binary framing. An
intentional ABI or example change therefore requires an explicit fixture and
provenance update.
