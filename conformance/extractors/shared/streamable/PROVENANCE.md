# Streamable fixture provenance

`success.json` is an attributable synthetic fixture authored for the Go port.
Its field names, status handling, title preference, protocol-relative URL
normalization, and format mapping were derived by inspecting
`yt_dlp/extractor/streamable.py` in the read-only upstream checkout pinned at
`aefce1eea4d0b6bab1ec2bd3beff09bff91a39c8`.

The values, hostnames, identifiers, and media URLs are synthetic and contain no
upstream copyrighted media, credentials, or user data. Runtime and build-time
behavior remains entirely native Go and does not access the reference checkout.
