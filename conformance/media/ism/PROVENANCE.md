# ISM fixture provenance

`manifest.ismc` is a deterministic synthetic Smooth Streaming manifest. Its addressing tokens and fragment ordering were derived by inspection of `yt_dlp/downloader/ism.py` in the read-only pinned upstream checkout at `aefce1eea4d0b6bab1ec2bd3beff09bff91a39c8`. It contains no upstream media or Python runtime dependency.
