# Reference-derived POSIX shlex and recursive configuration fixture
--output "root # title.%(ext)s"
--config-locations child.conf
--alias audio,-X "-f {0} -x"
--audio bestaudio
--retries 2
--retries 4
